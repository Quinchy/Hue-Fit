/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["geist"],
  images: {
    domains: ['abvikodzzsvmdljgcdhq.supabase.co'],
  },
};

export default nextConfig;
